services:
  server:
    build: ./server
    env_file:
    - ./server/.env
    ports:
      - "3000:3000"

  client:
    build: ./client
    ports:
      - "8080:80"
    depends_on:
      - server

volumes:
  dbdata: